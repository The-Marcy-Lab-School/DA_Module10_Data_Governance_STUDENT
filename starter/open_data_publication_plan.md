# Open-Data Publication Plan (objective 5 — with a real partner)

**Do this one with a real partner** — a real, lighter-weight
collaboration than some other modules (a shared doc or a joint editing
session is fine; no git workflow needed since `git-version-control`
isn't newly tested this module). Both partners' names go in
`required_components.md`.

**The real given scenario**: your city wants to publish real 911
call-response data publicly, including each call's exact incident
address and response time, to support an open-government transparency
initiative.

## 1. What must be redacted or aggregated before public release

TODO (real, specific, with a partner): exact street addresses are a
real geospatial precision risk (`gis-geospatial-analysis`'s own real
concern) — what real aggregation level actually resolves this (block-
level? census tract? a real, specific choice, not "make it vaguer")?
Are there other real fields in this scenario that need redaction
(e.g. a caller's name/phone number, if present)?

## 2. Why this aggregation level, specifically

TODO: what real re-identification risk remains at your chosen level,
and why is that an acceptable real tradeoff against the public
transparency goal? A real, reasoned tradeoff — not just "this seems
safe."

## 3. Real metadata that must accompany the release

TODO: what would a real external user, with zero context, need to
understand this dataset's real scope and limitations without contacting
your team (`documentation`'s own real bar, reinforcing Module 8)? Real
refresh cadence, real known limitations, a real contact/owner.

## Partners

- Partner 1:
- Partner 2:
