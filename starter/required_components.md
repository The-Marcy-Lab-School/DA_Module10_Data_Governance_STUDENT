# Required Components

Fill this in **as you go**, with real evidence — not from memory at the
end.

## 1. Your real dataset and regulation

- Your real, chosen dataset (source — likely your own Module 3/8/9
  domain data):
- The real, specific, named regulation that applies (HIPAA, FISMA/NIST
  RMF, CJIS, GLBA, or another real, specific framework for your
  domain) — and why it applies:

## 2. Field-by-field classification (objective 1's real rep, applied independently)

- Confirm every field in `data_contract.schema.yaml` has a real
  `x-sensitivity-tier` AND a real, specific `x-regulatory-basis` — not
  a uniform tier applied to everything:

## 3. The real data contract — validated, not just plausible

- Paste the real output of validating your schema (see
  `GETTING_STARTED.md` for the exact real `jsonschema` command):
- Ownership/stewardship metadata (owner, refresh cadence, business
  glossary term) for at least 3 real fields:

## 4. The real ERD

- Confirm `erd.mmd` renders without a real syntax error (paste into
  [mermaid.live](https://mermaid.live) or view via `draw.io`) — paste
  confirmation:
- Confirm the ERD and the data contract **agree with each other** on
  every field's real tier — no contradictions
  (`common_project_mistakes`' own #4):

## 5. Regulation gap memo (objective 4's real output)

- Link to (or confirm inline in) `regulation_gap_memo.md` — filled in
  for real:

## 6. Open-data publication plan (objective 5's real output, with a partner)

- Link to (or confirm inline in) `open_data_publication_plan.md` —
  filled in for real, both partners named:
